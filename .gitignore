# Rust build output
/target

# Staged rootfs + initramfs + generated images
/build

# Vendored archives regenerated by scripts/build.sh from the toolchain
/vendor/*.a

# The XIP kernel is its own repository — do not vendor it here.
# Point scripts/mkimage.sh at it with LWRT_KERNEL=...
/mips-xip-kernel

# Editor / OS noise
*.swp
*~
.DS_Store
